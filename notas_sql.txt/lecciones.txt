El curso se compone de 20 capítulos. 
Este documento tiene el objetivo de mantenerse principalmente conceptual. 

Capítulo 1: Introducción. Se aborda la estructura y alcance del curso. Presente en el prefacio. 

Capítulo 2: Instalación de postgreSQL y google Big Query. Presente en el prefacio. 
    Big query. Hay costos por almacenado y por búsquedas. 
        Por lo tanto si haces una búsqueda la puedes guardar y asi no gastas múltiples veces por múltiples búsquedas solo preview. 

Capítulo 3: Fundamental SQL statements. 
    Crear bases de datos, insertar, importar, subir archivos, select, distinct, where, and-or-not, where, update, delete y alter. 
    
        Click on create in the database under a server in pgAdmin to create a database. 
        Name the database. You can right click and delete it (drop botton). Bueno en inglés pero se entiende. 
        
        tip: verbos en mayúsculas son mejores para diferenciar los queries. 

    Crear la tabla. 
        Click derecho en la base de datos seleccionar query tool. 
        Comando create_table, 
        definir las columnas, tipo de datos y consideraciones, 
        revisar en schemas, tables y ahí aparece
    
    Big Query 
    ---
    Insert data
        Tiene la estructura de INSERT INTO nombre_tabla VALUES #(siguiente renglon)
        (1, 'ef', 'de', 'ef@xyz.com') #registro a añadir
    
    Insert from file
    Se usa COPY nombre_de_tabla y FROM url (/ no \) #a veces hay temas de permisos de acceso. 

    En BigQuery se hace un upload con browse se hace skip de la columna informativa con autoesquema. 
        En casos específicos grandes archivos se importa desde drive o google cloud shell

    